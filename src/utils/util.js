
/* In this file we gather all the functions that will help us in our library*/

export const isPathAbsolute = (path) => {
  return true;
};

export const relToAbs = (path) => { 
/* path.resolve([...paths]) */
  const absPath = '';
  return absPath;
};

export const folderRecursion = (path) => { 
  const absPath = '';
  return absPath;
};